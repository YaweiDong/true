let getters = {
    user(state) {
        return state.login1;
      },
     //筛选用户
     user(state){
        return state.login1;
    },
      dizhi(state) {
        return state.address;
      }
}
export {
    getters
};
